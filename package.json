{
  "name": "secureplay-network-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack multiplayer networking and server-security case-study lab with a simulated secure control plane.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/store.js && node --check src/simulation.js"
  },
  "engines": {
    "node": ">=20"
  }
}
